#include <stdio.h>

//Compiler version gcc  6.3.0

int main()
{
  int age;
  int a=60;
  int b=50;
  printf("Enter your age:");
  scanf("%d",&age);
  if (age>=a)
  printf("Pension amount is Rs %d",10000);
  if (age>=50)
  printf("Pension amount is Rs %d",5000);
  else
  printf("Pension amount is Rs %d",0);
  return 0;
}
